SENTRY_DSN_WEBAPP=
COMMIT=
WEBSOCKETS_URI=ws://localhost:3000/api/graphql
GRAPHQL_URI=http://localhost:4000/
MAPBOX_TOKEN="pk.eyJ1IjoiYnVzZmFrdG9yIiwiYSI6ImNraDNiM3JxcDBhaWQydG1uczhpZWtpOW4ifQ.7TNRTO-o9aK1Y6MyW_Nd4g"
NETWORK_NAME="Ocelot.social"
# Support contact address shown in the UI (mirrors the backend SUPPORT_EMAIL). Read at runtime
# via nuxt-env, so it can change without a rebuild.
SUPPORT_EMAIL="hello@ocelot.social"
# Branding needs NO configuration — both variables below are overrides. Out of the box the webapp
# mirrors the backend's archives into `.branding-cache` and reads three roots, in this order:
#   .branding-cache               the sync cache. ALWAYS first and not part of the configurable path:
#                                 it mirrors the backend, which owns the list of brands. Written and
#                                 owned by server-middleware/branding-sync.js.
#   deployment/configurations     next to the running process. In the container that is
#                                 /app/deployment/configurations — where a branded build bakes its
#                                 archive, and where docker-compose mounts the brand sources in dev.
#   ../deployment/configurations  the same directory seen from a repo checkout, because `yarn dev`
#                                 runs from webapp/.
# The last two are the conventional locations (@ocelot-social/branding discover.DEFAULT_ROOTS); both
# are always tried, and one that does not exist costs a single failed readdir. Together they let ONE
# default be correct in the image and in a checkout. Anything found earlier shadows a later copy of
# the same brand id — nothing is ever deleted to settle that.
# Build them with `node packages/branding/scripts/build-dev-brandings.ts`; the active brand is switched
# live in the admin Branding tab, or pinned for dev with OCELOT_ACTIVE_BRANDING. Iterate on a single
# brand without a rebuild:
#   node packages/branding/scripts/build-brand-archive.ts <brand-dir> --watch
# then edit brand.config.ts — the backend serves the rebuilt archive at once and the webapp picks it up
# within OCELOT_BRANDING_SYNC_TTL_MS (60s; set it to 0 to see every change on the next refresh).
#
# OCELOT_BRANDING_ASSETS_DIR REPLACES the read locations above (`:`-separated, an earlier root shadows
# a later one per brand id — nothing is ever deleted to settle that). The cache stays first regardless.
# OCELOT_BRANDING_CACHE_DIR moves the cache; point it at a directory NOTHING else writes — never at
# deployment/configurations, or the sync would compete with your own brand builds.
# OCELOT_BRANDING_ASSETS_DIR=
# OCELOT_BRANDING_CACHE_DIR=
# OCELOT_BRANDING_SYNC_TTL_MS=
# OCELOT_ACTIVE_BRANDING=
