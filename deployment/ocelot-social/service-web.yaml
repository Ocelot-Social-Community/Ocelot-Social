apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: ocelot-social
  labels:
    human-connection.org/selector: deployment-human-connection-web
spec:
  ports:
    - name: web
      port: 3000
      targetPort: 3000
  selector:
    human-connection.org/selector: deployment-human-connection-web
