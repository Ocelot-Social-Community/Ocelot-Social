{
  "name": "@ocelot-social/branding",
  "version": "0.0.1",
  "description": "Shared branding override schema, framework defaults and resolver — consumed by both backend and webapp so brand-tunable constants (group limits, registration lengths, metadata, logo paths, …) cannot drift between the two.",
  "license": "MIT",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src"
  ],
  "sideEffects": false
}
