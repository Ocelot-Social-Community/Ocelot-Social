{
  "//": "Tooling config: type-checks the WHOLE package (src + scripts + tests) and is what ESLint's",
  "//2": "projectService and `npm run typecheck` use. It emits nothing — `tsconfig.build.json` does the",
  "//3": "CommonJS dist build. nodenext so the .mts scripts/tests (ESM, top-level await, import.meta) are",
  "//4": "type-checked in their real module format; src stays CommonJS (no package `type`).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "noEmit": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src/**/*.ts", "scripts/**/*.mts"]
}
