import{_ as o}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as t,a as n,o as c}from"./app-weOYQ1UP.js";const a={};function l(r,e){return c(),t("div",null,e[0]||(e[0]=[n('<h1 id="video-call-e2e-prejoin-only-no-livekit-connect" tabindex="-1"><a class="header-anchor" href="#video-call-e2e-prejoin-only-no-livekit-connect"><span>Video-call e2e (PreJoin only, no LiveKit connect)</span></a></h1><p>What these specs cover:</p><ul><li>The video-call button only renders for public-group members with <code>videoCallConfig.enabled === true</code>.</li><li>The PreJoin dialog opens / cancels correctly.</li><li>The mic and camera toggles flip the UI state.</li></ul><p>What they do <strong>not</strong> cover:</p><ul><li>A real LiveKit <code>room.connect()</code> / WebRTC media exchange.</li><li>The error-block / Retry / Back-to-PreJoin paths. Those used to live here but depended on the backend returning an <em>unreachable</em> LiveKit URL. Locally the URL comes from <code>backend/.env</code> and may point at a real LiveKit instance, so the connect call succeeds and the error block never renders. Reintroduce these scenarios only when we can stub the <code>joinGroupVideoCall</code> GraphQL response (e.g. via <code>cy.intercept</code>) so the test fully owns the failure condition.</li></ul><p>How the browser plays along:</p><ul><li><code>cypress.config.js</code> adds <code>--use-fake-ui-for-media-stream</code>, <code>--use-fake-device-for-media-stream</code>, and <code>--autoplay-policy=no-user-gesture-required</code> for the chrome / chromium browsers. Electron is the Cypress default and <strong>does not honour launchOptions.args</strong> (you&#39;ll see a &quot;browser launch options not supported by electron&quot; warning if you accidentally re-enable that branch). Cypress auto-grants media permissions in Electron, so the toggle steps still hit the regular code paths via the <code>permissionStatus === &#39;prompt&#39;</code> fallback.</li></ul><p>How the backend plays along:</p><ul><li><code>docker-compose.test.yml</code> injects synthetic <code>LIVEKIT_URL</code> / <code>LIVEKIT_API_KEY</code> / <code>LIVEKIT_API_SECRET</code> so that <code>videoCallConfig.enabled</code> is reported as <code>true</code> and the video-call button can be exercised. Note that, since we no longer exercise the connect path, the actual reachability of <code>LIVEKIT_URL</code> no longer matters for this spec.</li></ul>',9)]))}const d=o(a,[["render",l],["__file","index.html.vue"]]),p=JSON.parse('{"path":"/cypress/e2e/videoCall/","title":"Video-call e2e (PreJoin only, no LiveKit connect)","lang":"en-US","frontmatter":{},"headers":[],"git":{"createdTime":1788634591000,"updatedTime":1788634591000,"contributors":[{"name":"dependabot[bot]","email":"49699333+dependabot[bot]@users.noreply.github.com","commits":1}]},"readingTime":{"minutes":0.87,"words":262},"filePathRelative":"cypress/e2e/videoCall/README.md","localizedDate":"September 5, 2026","excerpt":"\\n<p>What these specs cover:</p>\\n<ul>\\n<li>The video-call button only renders for public-group members with\\n<code>videoCallConfig.enabled === true</code>.</li>\\n<li>The PreJoin dialog opens / cancels correctly.</li>\\n<li>The mic and camera toggles flip the UI state.</li>\\n</ul>\\n<p>What they do <strong>not</strong> cover:</p>"}');export{d as comp,p as data};
