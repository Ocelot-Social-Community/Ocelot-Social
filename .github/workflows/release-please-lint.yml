name: Release Please Config Lint

# Three groups of paths, and all three have to be here:
#
#   the configs       — the obvious one.
#   the workflows     — one invariant is that exactly one workflow reads each config, and that can
#                       be broken by editing a workflow alone, which is precisely how the
#                       @ocelot-social/ui@0.0.2 publish was lost.
#   the bump targets  — every file the configs claim to version. The lockstep check compares those
#                       against the manifest, so a hand-edited version in backend/package.json or a
#                       deleted `x-release-please-version` marker in a chart has to start a run.
#                       Without them the drift would only surface on the next release pull request,
#                       landing on whoever merges the release rather than on whoever caused it.
#
# The third group duplicates the `extra-files` of the configs, and `push`/`pull_request` then
# duplicate each other — a YAML anchor would be the obvious fix and does NOT work here, because the
# Actions workflow parser does not support anchors and aliases. The failure mode of trying is the
# worst kind: the trigger silently never matches and the workflow simply stops running. So the list
# is written out twice, and `lint.mjs` checks that BOTH copies still cover every bump target — see
# the `lint workflow triggers` check there.
on:
  push:
    branches: [master]
    paths:
      - '.github/release-please/**'
      - '.github/workflows/**'
      - 'package.json'
      - 'backend/package.json'
      - 'backend/package-lock.json'
      - 'webapp/package.json'
      - 'webapp/package-lock.json'
      - 'maintenance/package.json'
      - 'maintenance/package-lock.json'
      - 'deployment/helm/charts/ocelot-social/Chart.yaml'
      - 'deployment/helm/charts/ocelot-neo4j/Chart.yaml'
      - 'packages/ui/package.json'
      - 'packages/branding/package.json'
      - 'packages/branding/src/version.ts'
  pull_request:
    branches: [master]
    paths:
      - '.github/release-please/**'
      - '.github/workflows/**'
      - 'package.json'
      - 'backend/package.json'
      - 'backend/package-lock.json'
      - 'webapp/package.json'
      - 'webapp/package-lock.json'
      - 'maintenance/package.json'
      - 'maintenance/package-lock.json'
      - 'deployment/helm/charts/ocelot-social/Chart.yaml'
      - 'deployment/helm/charts/ocelot-neo4j/Chart.yaml'
      - 'packages/ui/package.json'
      - 'packages/branding/package.json'
      - 'packages/branding/src/version.ts'

# One run per workflow and branch. A newer push makes the older run's result irrelevant, so it is
# cancelled instead of holding a runner. `event_name` is part of the group on purpose: a branch
# push and its pull_request run are separate required checks — they must not cancel each other.
# Keyed by pull request NUMBER, not `head_ref`: head_ref is only the branch NAME, which is unique
# within this repository but not across forks — two contributors using GitHub's "edit this file"
# flow both get `patch-1`, and their pull requests would then cancel each other's runs. `github.ref`
# stays as the fallback for events that carry no pull request (a push gives refs/heads/<branch>).
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Nothing here writes, and nothing needs the token at all — the checkout is public repository
# content. Stated explicitly so the job cannot inherit a read-write repository or organisation
# default.
permissions:
  contents: read

jobs:
  lint:
    name: Lint release-please config
    runs-on: ubuntu-latest
    steps:
      # Deliberately NOT a sparse checkout. The lint does not only read this directory: the lockstep
      # check opens every file the configs claim to bump — package.json and package-lock.json in
      # backend, webapp and maintenance, both Helm charts, each package's own manifest — and that
      # list is defined by the configs, so no static `sparse-checkout` can stay correct. An earlier
      # version of this workflow checked out `.github` alone and the lint reported all fourteen of
      # those files as missing.
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: '.nvmrc'

      # Into a throwaway prefix, never into the repository. release-please is a release tool, not an
      # application dependency; adding it to the root package.json to satisfy a lint would put it
      # into the dependency tree of everything built from this repository.
      #
      # Exact versions, because the point of the check is to compare against a KNOWN schema. Bumping
      # release-please here is a deliberate act that may legitimately turn the lint red — that is
      # the signal that an option moved or was dropped, and it is better to see it here than in a
      # release run. Keep it in the same major as the release-please-action pinned in the release
      # workflows (v5.0.0 depends on `release-please@^17.6.0`); a newer patch only ever validates
      # more strictly than what the action bundles, which is the safe direction.
      - name: Install lint dependencies
        run: npm install --prefix "${RUNNER_TEMP}/rp-lint" --no-audit --no-fund --no-save ajv@8.20.0 ajv-formats@3.0.1 jsonpath-plus@10.3.0 yaml@2.9.1 release-please@17.11.2

      - name: Lint release-please config
        run: node .github/release-please/lint.mjs
        env:
          RP_LINT_MODULES: ${{ runner.temp }}/rp-lint
