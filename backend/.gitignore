node_modules/
.env
.vscode
.idea
yarn-error.log
build/*
coverage.lcov
.nyc_output/
!.gitkeep

# Generated GraphQL API docs — build artifacts, regenerated by `npm run docs:api`
public-docs/

# NOTE: schema.graphql is deliberately NOT ignored. It is generated by `npm run schema:print`,
# but it is committed so that changes to the public API surface appear as a reviewable diff
# and can be guarded by src/graphql/schema.snapshot.spec.ts.

# Apple macOS folder attribute file
.DS_Store
