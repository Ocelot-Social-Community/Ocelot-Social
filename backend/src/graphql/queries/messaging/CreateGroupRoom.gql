mutation CreateGroupRoom($groupId: ID!) {
  CreateGroupRoom(groupId: $groupId) {
    id
    roomId
    roomName
    isGroupRoom
    lastMessageAt
    unreadCount
    users {
      id
      name
    }
  }
}
