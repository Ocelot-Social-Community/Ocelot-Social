"Relationship representing a user's emotional reaction to a post."
type EMOTED {
  "The reacting user."
  from: User
  "Alias of `from`, kept so removing neo4j-graphql-js did not change the published API."
  User: User @deprecated(reason: "Use `from`.")
  "The post reacted to."
  to: Post

  emotion: Emotion
  createdAt: String
  updatedAt: String
}

"Selects which emotion to add or remove on a post."
input _EMOTEDInput {
  emotion: Emotion
  createdAt: String
  updatedAt: String
}

input _PostEMOTEDFilter {
  emotion_in: [Emotion]
  createdAt: String
  updatedAt: String
}

type Mutation {
  "Add the current user's emotional reaction to a post. Requires authentication."
  AddPostEmotions(to: _PostInput!, data: _EMOTEDInput!): EMOTED
  "Remove the current user's emotional reaction from a post. Requires authentication."
  RemovePostEmotions(to: _PostInput!, data: _EMOTEDInput!): EMOTED
}
