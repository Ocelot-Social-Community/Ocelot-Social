# `vuep` is pinned to a fork branch (git+https://github.com/visualjerk/vuep.git#fix-iframe-firefox)
# because upstream never merged the Firefox iframe fix. npm 12 refuses git dependencies unless they
# are opted into explicitly (EALLOWGIT); yarn 1 fetched them without asking.
allow-git=true
